# NodeJS-code-sample

### Structure:
`app.js` - main file. There is creating a server and routes are registered;  
`login.js` - routing of a login page;  
`cache.js` - some functions for the cache;  
`cache.test.js` - unit tests for the cache functions.  
